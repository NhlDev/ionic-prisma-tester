import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-prisma-tester',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
